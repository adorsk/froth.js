if (typeof define !== 'function') { var define = require('amdefine')(module) }

define(['froth'], function(Froth){
    Froth.set({
        'body': {
            'background-color': 'blue'
        }
    });
});
